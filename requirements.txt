# CSC14006 — Facial Landmark Localization (Manim explainer)
# pip install -r requirements.txt
#
# Also required on the system (not pip):
#   - ffmpeg on PATH
#   - OpenGL (GPU / driver)
#   - Font "Cascadia Mono" — for text identical to the published video.
#     Free: https://github.com/microsoft/cascadia-code (ships with Windows Terminal / VS Code).
#     Without it Manim falls back to another monospace font: same content, slightly
#     different glyph shapes.
#
# Render one shot:   manimgl source/shotXX_name.py SceneClass -w --hd
# Render full video: bash render_all.sh   ->  videos/ch12_final.mp4

manimgl==1.7.2
